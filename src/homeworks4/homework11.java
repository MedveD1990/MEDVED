package homeworks4;

import java.util.Arrays;

public class homework11 {
    public static void main(String[] args) {
        String [] fruits = {"Apple", "Orange", "Watermelon", "Melon"};
        String [] friuts1 = {"Kiwi "};
        for (int i=0; i<4; i++);
        System.out.println(fruits[1] + ", " + fruits[3]);
        System.out.println(Arrays. toString(fruits));
        Arrays.sort (fruits);
        System.out.println(Arrays. toString(fruits));
        // Я пытался заменить третий фрукт на иной с помощью метода System.arraycopy, но выдавало ошибку.

    }
}
